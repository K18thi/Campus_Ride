import express from 'express';import cors from 'cors';import dotenv from 'dotenv';import routes from './routes/index.js';dotenv.config();const app=express();app.use(cors());app.use(express.json());app.get('/api/health',(_,res)=>res.json({status:'ok'}));app.use('/api',routes);app.use((err,_,res,__)=>res.status(500).json({message:err.message||'Unexpected server error'}));const port=process.env.PORT||5000;if(process.env.NODE_ENV!=='test')app.listen(port,()=>console.log(`CampusRide API on ${port}`));export default app;
